

FIXES NEEDED: 
	 CAT: fin the reason why cat is missing some byts while reading and fix it.
	 CP: create the function from start so it could function the best.
	 OctoShell: make the open multipile procceses when in parrlow while creating a pipe and make the *filetype and enviromental variables. (v)
	 GLOBAL: Add check for succesful allocation in sepIntoCommand functio!!!!!!
			 make error systems.
			 add records for the codes so that it will be easy to understand.
			 Need to add ALIAS oprion and fix the quotes ("") system so when there are quotes it wouldn't be part of it.
			 Add a config file for variables between sessions, SET function DEL function and more, and set the a variable in the start of a command will reffer only to the command.
			 Make the parser better by creating a token structure and reffrence each token type by enaum. and support mroe variations of set variables like bash does.
			 Add signals and more jobs like auto compelete when pressing tab or view history and more.
			 Add a script language:) that will make your shell a ture masterpiece.
			 Add an option to run scripts of your shell like interperter.

			 
